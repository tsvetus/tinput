import {INPUT} from '../styles';

export default {

    container: {
        ...INPUT.CONTAINER,
        justifyContent: "space-between"
    },

    label: {
        ...INPUT.LABEL
    },

    edit: {
        transform: "scale(1.5)",
        alignSelf: "center"
    }

}
