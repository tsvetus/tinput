import {COLOR} from '../styles';

export default {
    display: "flex",
    backgroundImage: "linear-gradient(" + COLOR.PANEL + ", " + COLOR.WINDOW + ")",
    backgroundColor: COLOR.PANEL,
    padding: "8px"
}
